<!-- views/booking.ejs -->
<!-- ---------------------------------------------------------
   📄 Booking Page — Car Rental & Improvement Lessons
--------------------------------------------------------- -->
<section class="container py-5">

	<!-- Main Page Heading -->
	<h1 class="text-center mb-5"><%= __('booking.title', 'Veuillez sélectionner un service') %></h1>
	<meta name="preselected-service" content="<%= preselect || '' %>">

	<!-- Service Selection -->
	<div class="row justify-content-center g-4">

		<!-- Car Rental -->
		<div
			class="col-md-5"
		>
			<div
				class="card service-card p-3 text-center"
				role="button"
				tabindex="0"
				aria-pressed="false"
				data-form="rental"
			>
				<img
					src="/images/services/car_rental.png"
					alt="<%= __('booking.car_rental_alt', 'Service de location de voiture pour l’examen de la SAAQ') %>"
					class="img-fluid mb-3"
					loading="lazy"
				/>
				<h3 class="fs-4"><%= __('booking.car_rental_title', 'Location de voiture pour examen SAAQ') %></h3>
				<p class="small text-muted mb-0">
					<%= __('booking.car_rental_desc', 'Louez un véhicule certifié pour votre examen pratique SAAQ avec un instructeur professionnel.') %>
				</p>
			</div>
		</div>

		<!-- Improvement Lessons -->
		<div
			class="col-md-5"
		>
			<div
				class="card service-card p-3 text-center"
				role="button"
				tabindex="0"
				aria-pressed="false"
				data-form="improvement"
			>
				<img
					src="/images/services/improvement_lessons.png"
					alt="<%= __('booking.improvement_alt', 'Cours d’amélioration de conduite') %>"
					class="img-fluid mb-3"
					loading="lazy"
				/>
				<h3 class="fs-4"><%= __('booking.improvement_title', 'Cours d’amélioration de conduite') %></h3>
				<p class="small text-muted mb-0">
					<%= __('booking.improvement_desc', 'Améliorez vos habiletés de conduite et préparez-vous pour votre examen routier avec des moniteurs certifiés.') %>
				</p>
			</div>
		</div>
	</div>

	<!-- Hidden heading for form section -->
	<h2 class="visually-hidden"><%= __('booking.form_section_title', 'Complétez votre réservation') %></h2>

	<!-- Booking Forms -->
	<div class="row mt-5 justify-content-center">

		<!-- Car Rental Form -->
		<div id="form-rental" class="col-md-7 booking-form d-none">
			<%- include('partials/bookings/car_rental_form') %>
		</div>

		<!-- Improvement Lessons Form -->
		<div id="form-improvement" class="col-md-7 booking-form d-none">
			<%- include('partials/bookings/improvement_lessons_form') %>
		</div>

	</div>

</section>
